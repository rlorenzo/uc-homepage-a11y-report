# Run lint/quality checks on every commit. Blocks the commit on failure.

set -e

echo "→ Biome check (lint + format, JS and CSS)"
npx --no -- biome check

echo "→ Fallow audit (dead code, duplication, complexity — new issues only)"
npx --no -- fallow audit

echo "✓ pre-commit checks passed"
